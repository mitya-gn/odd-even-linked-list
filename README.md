# odd-even-linked-list
Rearranges a singly linked list by grouping nodes at odd indices first, followed by nodes at even indices, while maintaining their original relative order. Uses two pointers to separate odd and even nodes efficiently, then connects both groups. Runs in O(n) time and uses O(1) extra space.
